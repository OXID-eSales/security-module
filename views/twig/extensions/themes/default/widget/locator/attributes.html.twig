{% extends 'widget/locator/attributes.html.twig' %}

{% block widget_locator_attributes_filterlist_hidden_params %}
    {% if oViewConf.isGetFormStripStokenEnabled() %}
        {{ oViewConf.getHiddenParamsOnly()|raw }}
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}

{% block widget_locator_attributes_resetfilter_hidden_params %}
    {% if oViewConf.isGetFormStripStokenEnabled() %}
        {{ oViewConf.getHiddenParamsOnly()|raw }}
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
