{% extends 'widget/header/search.html.twig' %}

{% block widget_header_search_form_hidden_params %}
    {% if oViewConf.isGetFormStripStokenEnabled() %}
        {{ oViewConf.getHiddenParamsOnly()|raw }}
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
